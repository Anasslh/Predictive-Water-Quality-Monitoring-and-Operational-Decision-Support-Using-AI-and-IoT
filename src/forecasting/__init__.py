"""
src/forecasting — Generic multi-step recursive forecasting module.

Public surface
--------------
frequency_detector.detect_frequency()       → timedelta
frequency_detector.compute_n_steps()        → int
recursive_forecaster.MultiStepForecaster    → forecast(window, n_steps)
feature_engineering_generic.build_features_time_aware()
"""
